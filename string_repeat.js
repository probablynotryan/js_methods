const someString = "right about now, funk soul brother. ";

console.log(`string repeated three times: ${someString.repeat(3)}`);
// yup
console.log(`empty string: ${someString.repeat(0)}`);
// yup

// negative numbers cannot be used
// you can decimal numbers and will repeat the flor value of that number

console.log(`should repeat twice : ${someString.repeat(2.62)}`);

//sure does
